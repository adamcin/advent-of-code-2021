mod common;

fn read() -> (Algo, Image) {

    for (i, line) in common::read_test_input("data/day-20/input.txt").iter().enumerate() {
        if i == 0 {

        }
        if line.is_empty() {
            
            continue;
        }
    }

    (Algo {}, Image {})
}

struct Algo {
}

struct Image {
}